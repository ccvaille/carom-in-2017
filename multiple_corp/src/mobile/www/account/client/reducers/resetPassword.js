import * as ResetPasswordActionTypes from 'constants/ResetPasswordActionTypes';

const initialState = {

};

function resetPassword(state = initialState, action) {
    switch (action.types) {
        default:
            return state;
    }
}

export default resetPassword;
